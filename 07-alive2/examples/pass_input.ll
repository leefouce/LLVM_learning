; 压轴用：喂给 Ch5 的 Mul2Shl 插件，再用 alive-tv 验证「插件输出」是否正确。
; 只放【一个】2 的幂乘法 —— SMT 求解器对位向量乘法很吃力，多个叠加会超时
; （见 notes 第 5 节），单个能快速给出结论。
define i32 @f(i32 %x) {
  %a = mul i32 %x, 8        ; 插件会把它替换成 shl x, 3
  ret i32 %a
}
